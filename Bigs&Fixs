-- Primer gran bug

Cuando movia mi personaje a la izquierda/arriba todo bien, pero al realizar un movimiento a la derecha/abajo ocurria un bucle que movia el personaje infinitamente hasta morir.

Al encontrar la celda verde en j, la pinta azul y pinta j+1 verde. Pero en la siguiente iteración del loop, j avanza a j+1, encuentra la celda verde que acaba de poner, y la vuelve a mover a j+2. Así sucesivamente.

El codigo:

switch (direccion) {
                    case 0: // Derecha
                        for (int i = 0; i < tablero.length; i++) {
                            for (int j = 0; j < tablero[i].length; j++) {
                                if (tablero[i][j].equals(m.verde)) {
                                    tablero[i][j] = m.azul;
                                    if (j + 1 < tablero[i].length) {
                                        tablero[i][j + 1] = m.verde;
                                    } else {
                                        dead = false;
                                    }
                                }
                            }
                        }
                        break;
                    case 1: // Abajo
                        for (int i = 0; i < tablero.length; i++) {
                            for (int j = 0; j < tablero[i].length; j++) {
                                if (tablero[i][j].equals(m.verde)) {
                                    tablero[i][j] = m.azul;
                                    if (i + 1 < tablero.length - 1) {
                                        tablero[i + 1][j] = m.verde;
                                    } else {
                                        dead = false;
                                    }
                                }
                            }
                        }
                        break;
                    case 2: // Izquierda
                        for (int i = 0; i < tablero.length; i++) {
                            for (int j = 0; j < tablero[i].length; j++) {
                                if (tablero[i][j].equals(m.verde)) {
                                    tablero[i][j] = m.azul;
                                    if (j - 1 >= 0) {
                                        tablero[i][j - 1] = m.verde;
                                    } else {
                                        dead = false;
                                    }
                                }
                            }
                        }
                        break;
                    case 3: // Arriba
                        for (int i = 0; i < tablero.length; i++) {
                            for (int j = 0; j < tablero[i].length; j++) {
                                if (tablero[i][j].equals(m.verde)) {
                                    tablero[i][j] = m.azul;
                                    if (i - 1 >= 1) {
                                        tablero[i - 1][j] = m.verde;
                                    } else {
                                        dead = false;
                                    }
                                }
                            }
                        }
                        break;
                }

Solucion: 

Invertir el orden de busqueda, ya que ahora el for busca de derecha a izquierda cuando va para la derecha, o de abajo a arriba cuando va para abajo.

Ahora los for revisan justo en el lado contrario de la direccion a la que van.

Es un bug tonto pero que me he llevado un tiempo solucionando xd

case 0: // Derecha
                        for (int i = 0; i < tablero.length; i++) {
                            for (int j = tablero[i].length - 1; j >= 0; j--) {

case 1: // Abajo
                        for (int i = tablero.length - 1; i >= 0; i--) {
                            for (int j = 0; j < tablero[i].length; j++) {
